<?php
/**
 * HAvoice — نمونهٔ پیکربندی محلی / Production
 *
 * کپی کنید به: config/config.local.php
 * این فایل در .gitignore است و هرگز به GitHub push نشود.
 *
 * روی InfinityFree:
 *   1) MySQL Databases → ساخت دیتابیس (هاست معمولاً sqlXXX.infinityfree.com است، نه localhost)
 *   2) phpMyAdmin → Import فایل database_import.sql (ریشه‌ی پروژه، utf8mb4)
 *   3) این فایل را با مشخصات پنل پر کنید
 *   4) پوشه‌های storage/ و uploads/ باید نوشتنی باشند
 *   5) اختیاری: یک‌بار HA_DB_AUTO_SEED=true برای ورود دوره‌ها از data/*.php، بعد خاموش کنید
 */

if (!defined('HA_ROOT')) {
    exit('دسترسی مستقیم ممنوع است.');
}

/* ---- Database (الزامی برای Production کامل) ---- */
define('HA_DB_HOST', 'sqlXXX.infinityfree.com');
define('HA_DB_PORT', 3306);
define('HA_DB_NAME', 'epiz_XXXX_havoice');
define('HA_DB_USER', 'epiz_XXXX');
define('HA_DB_PASS', 'YOUR_DB_PASSWORD');

/* ---- Site ---- */
// define('HA_SITE_URL', 'https://your-domain.infinityfreeapp.com');
// define('HA_BASE_PATH', '');
// define('HA_PRETTY_URLS', false);
// define('HA_FORCE_HTTPS', true);
// define('HA_DEBUG', false);

/* ---- Seed خودکار در نخستین اتصال موفق (فقط اگر جداول خالی باشند) ---- */
// define('HA_DB_AUTO_SEED', true);

/* ---- ادمین اولیه هنگام seed (اگر هیچ کاربری نباشد) ---- */
// define('HA_SEED_ADMIN_EMAIL', 'admin@example.com');
// define('HA_SEED_ADMIN_PASSWORD', 'ChangeMe_Strong_8+');
// define('HA_SEED_ADMIN_NAME', 'مدیر');

/* ---- آپلودِ فایل (ویدیو، پادکست، PDF، تصویر) ----
   فایل‌ها در uploads/ با نامِ تصادفی و پسوندِ تأییدشده ذخیره می‌شوند و
   اجرای اسکریپت در آن پوشه با .htaccess مسدود است.
   مقدارِ واقعی هرگز از upload_max_filesize / post_max_sizeِ PHP بیشتر
   نمی‌شود؛ برای ویدیوی بزرگ روی میزبانیِ اشتراکی، پیوندِ آپارات/یوتیوب
   (امبدِ مجاز) را در فیلدِ «نشانی» بگذارید. */
// define('HA_UPLOAD_MAX_BYTES', 8388608);        // ۸ مگابایت برای PDF و تصویر
// define('HA_UPLOAD_MAX_MEDIA_BYTES', 26214400);  // ۲۵ مگابایت برای ویدیو/صوت (۰ = همان سقفِ عمومی)
