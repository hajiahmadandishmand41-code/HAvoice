# تنظیمات — فقط از سمت PHP خوانده می‌شود، هرگز از وب
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
Options -Indexes
