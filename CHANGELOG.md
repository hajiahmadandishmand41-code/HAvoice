# تغییراتِ HAvoice

این فایل خلاصه‌ی تغییراتِ هر نسخه است. شماره‌ی نسخه‌ی جاری در `config/config.php`
(`HA_VERSION`) و در `<meta name="generator">` هر صفحه دیده می‌شود.

## ۳٫۴٫۰ — قفلِ bootstrap مدیر و فرم‌های ساده‌ی محتوا

* **اولین کاربر = مدیر فقط در نصب اولیه:** اگر هیچ کاربری در DB/فایل نباشد، همان ثبت‌نام نقش `admin` می‌گیرد و پرچم در JSON و جدول تنظیمات قفل می‌شود.
* **بعد از قفل، هیچ کاربر جدیدی خودکار مدیر نمی‌شود** — حتی اگر فهرست کاربران خالی به‌نظر برسد.
* **دسترسی پنل فقط با `role=admin` ذخیره‌شده**؛ حدس «اولین رکوردِ فایل» حذف شد.
* فرم‌های دوره/درس/تمرین/کتاب/ویدیو/پادکست ساده‌تر شدند (نام + توضیح + فایل؛ بقیه اختیاری و خودکار).
* جابه‌جایی ترتیب تمرین (`admin_exercise_move`)، نظرات بدون DB در فایل، و آپلود با بررسی پسوند/MIME/امضا/مسیر.

## ۳٫۳٫۰ — لایه‌ی یادگیری، سازنده‌ی دوره و آپلودِ رسانه

تمرکزِ این نسخه: کاربر همیشه بداند **«الان کجام؟ درسِ بعدی چیست؟ تمرینِ بعدی چیست؟»**
و مدیر بتواند بدونِ دست‌زدن به کد، محتوا و فایل بسازد.

### افزوده‌ها
* **لایه‌ی یادگیریِ سمتِ سرور** (`includes/progress.php`, `includes/learning_ui.php`,
  `pages/progress.php`, `assets/css/learning.css`): سه وضعیتِ درس
  (انجام‌نشده / در حالِ مطالعه / تکمیل‌شده)، جریانِ یادگیری، قرص‌های وضعیت، نوارِ
  چسبانِ «قدمِ بعدی» و صفحه‌ی «پیشرفتِ یادگیری».
* **مسیرِ یادگیریِ هر دوره و درس** (`course_learning_path`, `lesson_learning_path`):
  ترتیبِ واقعیِ درس‌ها و تمرین‌ها، درسِ قبلی/بعدی، تمرینِ مرتبط و درصدِ پیشرفت.
* **پردازشِ POST پیشرفت** (`includes/handlers/progress.php`, الگوی PRG با CSRF):
  «تکمیل شد»، «در حالِ مطالعه»، «پاک‌کردنِ پیشرفتِ دوره» و هدایت به
  درسِ بعدی / تمرینِ بعدی / صفحه‌ی دوره.
* **سازنده‌ی دوره در پنل** (`pages/admin/course_edit.php` + ماژولِ `adminBuilderModule`
  در `assets/js/main.js`): ویرایشِ مرحله‌ها و درس‌ها به‌صورتِ ردیف‌های آماده، تغییرِ
  ترتیب با ↑/↓ و کشیدن‌ورهاکردن، افزودنِ ردیف/مرحله بدونِ رفرش، و تبدیلِ خودکارِ متنِ
  ساده به بلوکِ محتوا (`ol`, `h2`, `ul`, `tip`). JSONِ خام فقط در بخشِ «پیشرفته».
* **ترتیبِ تمرین‌ها**: فیلدِ «ترتیب» در فرمِ تمرین، ستونِ ترتیب/دوره در فهرستِ پنل و
  چیدمانِ (گروه، جایگاهِ درس، ترتیب، شاخص) در `exercises_for_course_ordered()`.
* **آپلودِ ویدیو و پادکست** (`includes/uploads.php` + فرم‌های `video_edit`/`audio_edit`):
  میزبانیِ فایل روی خودِ سایت به‌جای اجبارِ پیوندِ بیرونی؛ اگر فایل آپلود شود فیلدِ
  «نشانی» اختیاری است.
* **نمایشگرِ PDF درون‌سایتی** در صفحه‌ی کتاب (`pdf_viewer`) + دکمه‌ی دانلود.
* **پیام‌های تماس در MySQL** (`ha_contact_messages`) با `db_table_exists()` و
  نسخه‌ی پشتیبانِ CSV؛ فهرستِ پیام‌ها در پنل هر دو منبع را با نشانِ منبع نشان می‌دهد.
* **نامکِ خودکارِ فارسی‌پسند**: `ha_transliterate_fa()`, `ha_slug_from_title()` و
  `ha_unique_slug()` — عنوانِ فارسی به نامکِ لاتینِ خوانا تبدیل می‌شود و در صورتِ
  تکرار شماره می‌گیرد. فیلدِ نامک در همه‌ی فرم‌های پنل اختیاری شد.
* **ثابت‌های تازه‌ی پیکربندی**: `HA_UPLOAD_MAX_MEDIA_BYTES` (سقفِ جداگانه‌ی ویدیو/صوت؛
  `0` = همان سقفِ عمومی)، جدولِ `ha_progress` در `sql/002-schema.sql` و
  `tools/db-migrate.php`.
* **خودآزمایی**: بخشِ تازه‌ی «۶ج» در `tools/selfcheck.php` برای لایه‌ی یادگیری،
  سازنده‌ی دوره، آپلود، نامکِ خودکار و پیام‌های DB
  (جمعاً ۱۸۶ PASS / ۰ FAIL بدونِ `--http`).

### تغییرها
* صفحه‌ی دوره و درس بازنویسی شدند تا ترتیبِ خواسته‌شده رعایت شود:
  دوره → درسِ فعلی → درسِ بعدی → تمرین → تمرینِ بعدی.
* دکمه‌های یکتا و هم‌نام در همه‌ی صفحه‌ها: «درسِ بعدی»، «تمرین»، «ادامه‌ی یادگیری»،
  «بازگشت به دوره».
* صفحه‌ی اصلی و هدر، پیشرفتِ کاربر را سمتِ سرور رندر می‌کنند (بدونِ وابستگی به
  `localStorage`) و دسترسیِ سریعِ «پیشرفت» و «نظرات» کنارِ کلیدِ تم قرار گرفت.
* فرم‌های پنل ساده‌تر شدند: برچسبِ «نامک (خودکار اگر خالی)»، راهنمای نوعِ فایل
  (`ha_upload_kind_hint`) و حذفِ فیلدِ `required` از نامکِ کتاب/مقاله/پژوهش.
* پیامِ honeypotِ فرم تماس از «خطا» به «موفقیت» تغییر کرد تا ربات هم پاسخِ طبیعی ببیند
  و چیزی درباره‌ی مکانیزمِ ضدربات یاد نگیرد.

### رفعِ اشکال
* **هدایتِ «درسِ بعدی» درس‌ها را جا می‌انداخت**: حالا قدمِ بعدی بر پایه‌ی «جایگاهِ
  همان درسِ تکمیل‌شده» محاسبه می‌شود، نه اشاره‌گرِ کلیِ مسیر.
* **عنوانِ فارسی ⇒ نامکِ خالی ⇒ «عنوان الزامی است»**: ساختِ صوت/ویدیو/کتاب/مقاله/پژوهش
  با عنوانِ فارسی عملاً ممکن نبود؛ با نویسه‌گردانی و نامکِ پشتیبانِ
  `نوع-سالماهروز-ساعت` رفع شد.
* **نامکِ تکراری درس‌ها** در سازنده‌ی دوره: نامکِ درسِ موجود (با تطبیقِ عنوان) حفظ و
  برای مواردِ تازه از تکرار جلوگیری می‌شود.
* کارتِ ویدیو از `<a>` با `data-media-open` ساخته می‌شود تا بدونِ JavaScript هم
  فایل/امبد باز شود (پیش‌تر فقط با JS کار می‌کرد).

### امنیت
* سخت‌گیریِ بیشترِ `ha_safe_media_url()` در برابرِ path traversal.
* دفاعِ polyglot در آپلود: پسوند فقط از MIMEِ واقعیِ فایل (finfo) مشتق می‌شود.
* نقش‌ها فقط از دیتابیس/فایل خوانده می‌شوند (حذفِ حدسِ «اولین کاربرِ فایل = مدیر»).

---

برای تاریخچه‌ی ریزترِ تغییرات، `git log` مخزن منبعِ حقیقت است.
