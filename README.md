# های‌ویس — HAvoice

وب‌سایت آموزشی **فن بیان، سخنوری و مهارت‌های ارتباطی** — با PHP خام، HTML5، CSS3 و JavaScript خالص.
بدون فریم‌ورک، بدون Composer و بدونِ مرحله‌ی build؛ با **MySQL اختیاری** (در نبودِ دیتابیس، ذخیره‌سازیِ
فایل/JSON به‌صورتِ خودکار جایگزین می‌شود) و آماده‌ی میزبانی روی **InfinityFree** و هر هاست PHP دیگری.

---

## ۱) ویژگی‌ها

| بخش | مسیر | توضیح |
|---|---|---|
| صفحه‌ی اصلی | `index.php` (مسیر `home`) | هیرو، آمار پویا، نمای مسیر دوره، تازه‌ترین مقاله‌ها، تمرین‌ها، نکته‌ها، دعوت به اقدام |
| مسیر آموزشی مرحله‌ای | `index.php?p=course` | ۳ مرحله / ۱۰ درس، آزمونِ هر مرحله، پروژه‌ی نهایی، نوار پیشرفت، «چطور پیش برویم» |
| صفحه‌ی درس | `index.php?p=lesson&slug=…` | جریانِ یادگیری (وضعیتِ درس)، محتوا، جعبه‌ی تمرین با معیارِ سنجش، پیش‌نیاز و منابع، دکمه‌های «تکمیل شد / تمرین / درسِ بعدی»، نوارِ چسبانِ قدمِ بعدی و درسِ قبلی/بعدی |
| پیشرفتِ یادگیری | `index.php?p=progress` | «الان کجام؟» — درسِ جاری، درسِ بعدی، تمرینِ بعدی و درصدِ پیشرفتِ هر دوره (سمتِ سرور: MySQL یا `storage/admin/progress.json`) |
| حسابِ کاربری | `index.php?p=account` · `login` · `register` | ثبت‌نام/ورود با CSRF، honeypot و محدودیتِ نرخ؛ نقش‌ها (`admin`/`user`) در دیتابیس/فایل |
| پنلِ مدیریت | `index.php?p=admin` | مدیریتِ دوره/مرحله/درس/تمرین (با ترتیب)، مقاله، پژوهش، کتاب، ویدیو، پادکست، نکته، حوزه، کاربر، پیام‌ها و نظرات + آپلودِ فایل |
| ویدیو و پادکست | `index.php?p=videos` · `audios` | پخشِ درون‌سایتی (مودالِ ویدیو برای امبد/فایل، پخش‌کننده‌ی `<audio>` برای صوت) یا پیوندِ آپارات/یوتیوب/Vimeo |
| کتاب‌ها | `index.php?p=books&slug=…` | ورق‌زدنِ PDF **داخلِ خودِ سایت** (`<iframe>` روی فایلِ آپلودشده) + دکمه‌ی دانلود |
| مقاله‌ها | `index.php?p=articles` | فیلتر دسته (سمت سرور) + جستجوی زنده (سمت مرورگر) + مقاله‌ی ویژه |
| صفحه‌ی مقاله | `index.php?p=article&slug=…` | تایپوگرافی کامل، برچسب‌ها، مقاله‌های مرتبط، کپی نشانی |
| تمرین‌های عملی | `index.php?p=exercises` | ۲۴ تمرین با **تایمر**، **موضوع تصادفی**، چک‌لیست و شمارنده‌ی اجرا |
| نکته‌های کوتاه | `index.php?p=tips` | ۱۶ نکته، فیلتر موضوع، نکته‌ی تصادفی، ذخیره در مرورگر |
| درباره ما | `index.php?p=about` | مأموریت، ارزش‌ها، مخاطب، سؤالات متداول (آکاردئون) |
| تماس با ما | `index.php?p=contact` | فرم با CSRF + honeypot + محدودیت نرخ + ذخیره در فایل |
| جستجوی مقالات | `index.php?p=search&q=…` | جستجوی فارسی‌پسند روی عنوان/متن/برچسب + درس‌های مرتبط |
| ۴۰۴ | هر مسیر نامعتبر | با کد وضعیت ۴۰۴ واقعی و پیشنهاد محتوا |

* کاملاً **RTL** و **واکنش‌گرا** (موبایل، تبلت، دسکتاپ)، منوی همبرگری، حالت **تاریک/روشن/خودکار**.
* پیشرفتِ درس‌ها و تمرین‌ها **سمتِ سرور** ذخیره می‌شود (MySQL یا فایلِ JSON در `storage/`) تا با عوض‌شدنِ
  دستگاه یا پاک‌شدنِ کشِ مرورگر از بین نرود؛ نکته‌های نشانده‌شده همچنان در `localStorage` می‌مانند.
* دسترس‌پذیری: `skip-link`، `aria-*`، سلسله‌مراتب درست تگ‌ها، کنتراست مناسب، احترام به `prefers-reduced-motion` و کارکرد کامل فرم‌ها بدون JavaScript.

---

## ۲) ساختار فایل‌ها

```
HAvoice/
├── index.php                  نقطه‌ی ورود (هاست همین فایل را در ریشه باز می‌کند)
├── sitemap.xml                نقشه‌ی سایتِ ثابت و مرجعِ اصلی (Google Search Console همین را می‌خواند)
├── sitemap.php                نسخه‌ی پویایِ پشتیبان با نشانی مطلق + lastmod (خروجیِ تمیزِ XML)
├── robots.txt                 نسخه‌ی ثابت و مرجعِ اصلی (شامل خطِ Sitemap مطلق)
├── robots.php                 robots.txt پویایِ پشتیبان (خط Sitemap با دامنه‌ی واقعی)
├── .htaccess                  امنیت + فشرده‌سازی + 404 + سروِ مستقیمِ sitemap.xml/robots.txt + URL کوتاه (اختیاری)
├── vercel.json                استقرارِ بدونِ سرورِ ثابت (Vercel) — storage به مسیرِ موقت برمی‌گردد
├── config/
│   ├── config.php             تنظیماتِ پیش‌فرض: نام، ایمیل، سقف آپلود، محدودیت فرم، نسخه
│   └── config.local.php       (اختیاری، در .gitignore) — HA_DB_*، HA_SITE_URL، مدیرِ اولیه
├── sql/
│   └── 002-schema.sql         ساختارِ جداول: دوره/مرحله/درس، تمرین، مقاله، کتاب، رسانه،
│                              پژوهش، کاربران و نقش‌ها، پیشرفت، پیام‌های تماس، نظرات
├── includes/
│   ├── bootstrap.php          مسیریابی، سرصفحه‌های امنیتی، نشست، گیتِ احراز هویت، رندر
│   ├── helpers.php            escape، url، داده‌ها، جستجو، CSRF، رندر بلوک‌ها، نامکِ خودکار، rate-limit
│   ├── db.php                 لایه‌ی PDO (اختیاری) + توابع upsert و پیام‌ها
│   ├── repository.php         ذخیره‌سازیِ دوگانه: MySQL و آینه‌ی JSON در storage/admin
│   ├── auth.php               ثبت‌نام/ورود، نقش‌ها، مدیرِ اولیه (یک‌بار)، پیام‌های تماس
│   ├── progress.php           پیشرفتِ یادگیری، مسیرِ یادگیریِ دوره/درس، ترتیبِ تمرین‌ها
│   ├── uploads.php            آپلودِ امن (MIME با finfo، نامِ تصادفی، سقفِ حجم، راهنمای نوع)
│   ├── content.php            توابع دوره/درس/مقاله/تمرین/کتاب/رسانه (لایه‌ی محتوا)
│   ├── ui.php                 اجزای مشترک: کارت مقاله، نکته، تمرین، ویدیو، صوت
│   ├── learning_ui.php        جریانِ یادگیری، قرصِ وضعیت، نوارِ قدمِ بعدی، نمای کلیِ پیشرفت
│   ├── comments.php           نظرات/تجربیات (ثبت، صفِ تأیید، نمایش)
│   ├── icons.php              مجموعه آیکون SVG محلی + چاپ sprite (جایگزین ایموجی)
│   ├── meta.php               عنوان/توضیح/canonical/JSON-LD هر صفحه
│   ├── header.php             <head> + هدر + ناوبری + بنر صفحه + دسترسیِ سریع به پیشرفت
│   ├── footer.php             پاورقی + پوسته‌ی مودالِ رسانه + تنظیمات JS
│   └── handlers/              پردازشِ POST (الگوی PRG): contact، comment، register، login،
│                              logout، progress
├── pages/
│   ├── home.php  courses.php  course.php  lesson.php  progress.php
│   ├── articles.php  article.php  exercises.php  tips.php
│   ├── videos.php  audios.php  books.php  research.php
│   ├── account.php  login.php  register.php  comments.php
│   ├── about.php  contact.php  search.php  404.php
│   └── admin/                 داشبورد + CRUD هر نوع محتوا + سازنده‌ی دوره + کاربران + پیام‌ها
├── data/                      محتوای پایه (آرایه‌ی PHP؛ بدونِ نیاز به دیتابیس)
│   ├── site.php  categories.php  course.php  learning_paths.php
│   ├── articles.php  books.php  research.php  media.php
│   └── exercises.php  tips.php
├── assets/
│   ├── css/style.css          استایلِ پایه (توکن‌های رنگی WCAG، RTL، حالت تاریک، چاپ)
│   ├── css/learning.css       لایه‌ی یادگیری + سازنده‌ی دوره (موبایل‌اول، تاریک، چاپ)
│   ├── css/mobile-layout.css  تنظیمِ ریزِ چیدمانِ موبایل
│   ├── js/main.js             تم، منو، ماژولِ یادگیری، سازنده‌ی دوره، تایمر، مودالِ رسانه، جستجو
│   ├── js/theme.js            تعیین حالت تم پیش از رنگ‌آمیزی (بدون فلاش؛ سازگار با CSP)
│   ├── fonts/vazirmatn-var.woff2   فونت وزیرمتنِ متغیر، میزبانیِ محلی (+ OFL.txt)
│   └── img/                   بنر، عکس مدرس، favicon
├── uploads/                   فایل‌های آپلودشده‌ی مدیر (PDF/صوت/ویدیو/تصویر) — اجرا نمی‌شوند
├── tools/
│   ├── selfcheck.php          خودآزماییِ انتشار، بدون وابستگی (php tools/selfcheck.php)
│   └── db-migrate.php         ساختِ جداول و seed اولیه (php tools/db-migrate.php --seed)
└── storage/                   پوشه‌ی نوشتنی (دسترسی وب بسته است)
    ├── admin/                 آینه‌ی JSONِ محتوا و پیشرفت (وقتی DB نباشد)
    ├── messages/messages.csv  پشتیبانِ پیام‌های فرم تماس
    ├── rate-limit/            شمارنده‌ی ارسال (پنجره‌ی ثابت + GC)
    └── sessions/              نشست‌ها، فقط اگر save_path پیش‌فرض هاست خراب باشد
```

### تصاویرِ سایت (پوشه‌ی `assets/img/`)

| فایل | کجا نمایش داده می‌شود | اندازه |
|---|---|---|
| `fanbayan-banner.webp` | بنرِ بالای صفحه‌ی اصلی + تصویرِ اشتراک‌گذاری (`og:image`) | ۶۴۰×۲۳۳ |
| `instructor.jpg` | آواتارِ هیرو، بخشِ «معرفی مدرس» و صفحه‌ی «درباره» | ۳۶۰×۳۶۰ |
| `favicon.svg` | آیکونِ تبِ مرورگر | برداری |

برای عوض‌کردنِ عکس‌ها فقط همین دو فایل را با نامِ **همان** در `assets/img/` بگذارید؛ نیازی به ویرایشِ کد نیست.
بنر: نسبتِ ۶۴۰×۲۳۳ (برای وضوحِ بیشتر ۱۲۸۰×۴۶۶ یا ۱۹۲۰×۷۰۰ هم عالی است) —
عکسِ مدرس: مربعِ کامل، ۳۶۰×۳۶۰ (یا ۷۲۰×۷۲۰). خودآزمایی (`php tools/selfcheck.php`) اگر فایلی کم باشد
یا مسیری در کد اشتباه بماند، همان لحظه گزارش می‌دهد.

هر پوشه‌ی داخلی (`config/`, `data/`, `includes/`, `pages/`, `storage/`, `tools/`) یک
`.htaccess` با `Require all denied` دارد؛ این لایه‌ی دومِ محافظت است و در نبودِ
`.htaccess` ریشه هم کار می‌کند.

**اصل معماری:** `data/` فقط داده، `includes/` فقط منطق و قالب، `pages/` فقط چیدمان صفحه.
برای افزودن مقاله فقط یک قلم به `data/articles.php` اضافه کنید — فهرست، جستجو، منوی پاورقی، نقشه‌ی سایت و «مقاله‌های مرتبط» خودکار به‌روز می‌شوند.

---

## ۳) آپلود روی InfinityFree (گام‌به‌گام)

1. در [infinityfree.com](https://www.infinityfree.com) یک اکانت و سپس یک **Account** بسازید (دامنه‌ی رایگان زیرمجموعه مثل `yoursite.infinityfreeapp.com` هم کافی است).
2. وارد **Control Panel** شوید → بخش **Domains** → دامنه را به‌عنوان **Primary Domain** انتخاب و **Create Domain** را بزنید.
3. **Online File Manager** → وارد پوشه‌ی **`htdocs`** شوید و فایل پیش‌فرض (`index2.html`) را حذف کنید.
4. محتوای این مخزن را **داخل خودِ `htdocs`** بریزید؛ مسیر نهایی باید این‌طور باشد:
   ```
   htdocs/index.php          ✅ درست
   htdocs/HAvoice/index.php  ❌ غلط (یک سطح اضافه است)
   ```
   روش سریع: در GitHub → `Code ▸ Download ZIP`؛ فایل را در File Manager آپلود، انتخاب و **Extract** کنید؛ اگر پوشه‌ای مثل `HAvoice-main` ساخته شد، همه‌ی فایل‌های داخل آن را به `htdocs` **Move** کنید و بعد ZIP و پوشه‌ی خالی را حذف کنید.
5. در VistaPanel مرحله‌ی **Verify you are human** را انجام دهید تا سایت suspended نماند.
6. **Control Panel ▸ Website ▸ PHP Version** را روی **PHP 8.1 یا 8.2** بگذارید و Save کنید.
7. سایت را باز کنید: `https://yoursite.infinityfreeapp.com/` — صفحه‌ی اصلی باید نمایش داده شود.
8. مجوزها: روی `storage` راست‌کلیک ▸ **Permissions** ▸ `755` (فایل‌ها `644`). اگر نوشتن پیام خطا داد، موقتاً `775` فقط روی `storage`.
9. (توصیه‌شده) **SSL**: از بخش `SSL Certificates` گواهی رایگان بسازید؛ سپس در `.htaccess` بلوک «انتقال به https» را از کامنت خارج کنید و در `config/config.php` مقدار `HA_FORCE_HTTPS` را `true` بگذارید.
10. **سئو:** در `config/config.php` مقدار `HA_SITE_URL` را با دامنه‌ی نهایی پر کنید (مثلاً `https://yoursite.infinityfreeapp.com`). تا وقتی خالی است، `canonical` و `og:url` از میزبانِ درخواست ساخته می‌شوند که برای دامنه‌های چندگانه مطمئن نیست.
11. **خودآزمایی پس از نصب:** اگر روی هاست به خطِ فرمان دسترسی ندارید، موقتاً `HA_DEBUG` را `true` کنید و `tools/selfcheck.php` را از مرورگر باز کنید (سپس `HA_DEBUG` را به `false` برگردانید). در محیطِ محلی: `php tools/selfcheck.php --http`.

### نکته‌های مهمِ InfinityFree
* **کش/OPcache:** اگر تغییرات را ندیدید، چند دقیقه صبر کنید و کش مرورگر را با `Ctrl+F5` بشکنید.
* **`mail()` روی InfinityFree غیرفعال است**؛ به همین دلیل `HA_SEND_MAIL = false` است و پیام‌ها در فایل ذخیره می‌شوند.
* **دیدن پیام‌ها:** `storage/messages/messages.csv` را از File Manager دانلود کنید (با اکسل یا Notepad++ باز می‌شود). برای امنیت بیشتر، بعد از هر دانلود پوشه را خالی کنید.
* **محدودیت inode:** فایل ZIP و نسخه‌ی پشتیبان را روی هاست نگه ندارید.
* اگر میزبانی `.htaccess` را قبول نکرد، پوشه‌ی `storage` را یک سطح بیرون `htdocs` ببرید و `HA_STORE_MESSAGES` را `false` کنید.

---

## ۴) تنظیمات بعد از نصب

در `config/config.php`:

```php
define('HA_EMAIL',  'you@yourdomain.com'); // ایمیل نمایشی و پشتیبانی
define('HA_PHONE',  '۰۲۱ …');
define('HA_HOTLINE','+98…');              // برای لینک tel:
define('HA_STORE_MESSAGES', true);         // ذخیره‌ی پیام‌های فرم
define('HA_SEND_MAIL',      false);        // روی InfinityFree false بماند
define('HA_DEBUG',          false);        // true فقط برای تست محلی
define('HA_PRETTY_URLS',    false);        // حالت URL کوتاه (پایین)
define('HA_BASE_PATH',      '');           // نصب در پوشه‌ی فرعی: '/havoice'
define('HA_SITE_URL',       '');           // دامنه‌ی قطعی برای canonical/og:url/sitemap
define('HA_FORCE_HTTPS',    false);        // پس از فعال‌سازی SSL: true
define('HA_CSRF_TTL',       28800);        // عمر توکن CSRF (۸ ساعت)
define('HA_RATE_LIMIT_MAX', 3);            // حداکثر پیام در هر پنجره
define('HA_RATE_LIMIT_WINDOW', 600);       // طول پنجره (ثانیه)
define('HA_RATE_LIMIT_MIN_INTERVAL', 20);  // حداقل فاصله‌ی دو ارسال (ضد رگبار)
define('HA_RATE_LIMIT_MAX_FILES', 400);    // سقف فایل‌های شمارنده (inode هاست)
define('HA_UPLOAD_MAX_BYTES', 8388608);    // سقفِ آپلود (PDF/تصویر) = ۸ مگابایت
define('HA_UPLOAD_MAX_MEDIA_BYTES', 0);    // ۰ یعنی همان سقفِ عمومی؛ برای ویدیو/صوت سقفِ جدا
define('HA_VERSION', '3.3.0');
```

اتصالِ دیتابیس و مدیرِ اولیه در `config/config.local.php` تعریف می‌شوند (کپی از
`config/config.local.php.example`؛ این فایل در `.gitignore` است):

```php
define('HA_DB_HOST', 'sqlXXX.infinityfree.com');
define('HA_DB_NAME', 'epiz_XXXX_havoice');
define('HA_DB_USER', 'epiz_XXXX');
define('HA_DB_PASS', '…');
define('HA_DB_AUTO_SEED', true);           // seed خودکار در نخستین اتصالِ موفق
define('HA_SEED_ADMIN_EMAIL', 'admin@example.com');
define('HA_SEED_ADMIN_PASSWORD', 'ChangeMe_Strong_8+');
```

اگر دیتابیس پیکربندی نشود، همه‌چیز از `data/*.php` خوانده و در `storage/admin/*.json`
نوشته می‌شود؛ یعنی سایت بدونِ MySQL هم کامل کار می‌کند.

### پنلِ مدیریت، آپلود و پیشرفتِ یادگیری

* **اولین کاربر = مدیر (فقط یک‌بار):** اگر هیچ کاربری وجود نداشته باشد، نخستین ثبت‌نام
  نقشِ `admin` می‌گیرد و پرچمش در `storage/` ثبت می‌شود؛ پس از آن هیچ‌کس خودکار مدیر
  نمی‌شود. نقش‌ها در دیتابیس (یا `storage/users.json`) نگهداری و از پنلِ «کاربران» مدیریت
  می‌شوند. هیچ مسیر/فرمی احراز هویت را دور نمی‌زند.
* **سازنده‌ی دوره:** در «مدیریتِ دوره‌ها → ویرایش»، مرحله‌ها و درس‌ها به‌صورتِ ردیف‌های
  آماده (عنوان، دقیقه، ترتیب، متن) ویرایش می‌شوند؛ با دکمه‌های ↑/↓ (و کشیدن‌ورهاکردن
  در مرورگرِ جدید) ترتیب عوض می‌شود و متنِ ساده خودکار به بلوکِ محتوا (`ol`، `h2`، `ul`،
  `tip`) تبدیل می‌شود. JSONِ خام فقط در بخشِ «پیشرفته» باقی مانده است.
* **ترتیبِ تمرین‌ها:** هر تمرین فیلدِ «ترتیب» دارد؛ صفحه‌ی تمرین‌ها و نوارِ «تمرینِ بعدی»
  بر پایه‌ی (گروه، جایگاهِ درس، ترتیب، شاخص) چیده می‌شوند.
* **آپلودِ فایل:** در فرمِ کتاب (PDF + جلد)، پادکست (صوت) و ویدیو، فیلدِ آپلود کنارِ
  فیلدِ «نشانی» نشسته است؛ اگر فایل آپلود شود، نشانیِ خارجی لازم نیست. نوعِ فایل با
  **MIME واقعی (finfo)** سنجیده می‌شود (نه نامِ فایل)، نامِ ذخیره‌شده تصادفی است و
  `uploads/.htaccess` اجرای هر اسکریپتی را در آن پوشه می‌بندد.
* **نامکِ خودکار:** فیلدِ نامک در همه‌ی فرم‌ها اختیاری است؛ اگر خالی بماند از عنوان ساخته
  می‌شود — عنوانِ لاتین با خطِ تیره (`Hello World!` ⇒ `hello-world`) و عنوانِ فارسی با
  نویسه‌گردانی (`فن بیان` ⇒ `fn-byan`) و در صورتِ تکراری بودن با شماره (`…-2`).
* **پیشرفتِ یادگیری:** بازکردنِ صفحه‌ی درس، وضعیتِ «در حالِ مطالعه» را ثبت می‌کند و دکمه‌ی
  «تکمیل شد» وضعیتِ «انجام‌شد» می‌دهد؛ سپس کاربر دقیقاً به **درسِ بعدیِ همان ترتیب**
  (یا تمرینِ بعدی، یا صفحه‌ی دوره) هدایت می‌شود. در صفحه‌ی دوره/خانه/پیشرفت همیشه
  «درسِ فعلی، درسِ بعدی، تمرین، تمرینِ بعدی» دیده می‌شود.
* **پیام‌های تماس:** اگر جدولِ `ha_contact_messages` وجود داشته باشد پیام در MySQL ذخیره
  می‌شود و همیشه یک نسخه‌ی CSV هم در `storage/messages/` می‌ماند؛ فهرستِ پیام‌ها در پنل
  هر دو منبع را با نشانِ منبع نشان می‌دهد.

### URL کوتاه (اختیاری)
پیش‌فرض نشانی‌ها `index.php?p=articles&slug=power-of-pause` است و روی **هر** هاستی کار می‌کند. برای شکل `/articles/power-of-pause`:

1. `HA_PRETTY_URLS` را `true` کنید؛
2. در `.htaccess` خط `RewriteRule ^([a-z0-9_\-/]+)/?$ index.php?r=$1 [QSA,L]` را از کامنت خارج کنید؛
3. اگر ۴۰۴ گرفتید یعنی `mod_rewrite` غیرفعال است: خط را دوباره کامنت و مقدار را `false` کنید.

### افزودن محتوا

> راهِ اصلی، **پنلِ مدیریت** است (`index.php?p=admin`): دوره/مرحله/درس/تمرین، مقاله، پژوهش،
> کتاب (با آپلودِ PDF)، ویدیو، پادکست (با آپلودِ صوت)، نکته، حوزه و کاربر. ویرایشِ مدیر در
> MySQL (یا `storage/admin/*.json`) ذخیره می‌شود و بر محتوای پایه‌ی `data/` اولویت دارد —
> یعنی فایل‌های `data/` دست‌نخورده می‌مانند. ویرایشِ مستقیمِ فایل‌ها فقط برای استقرارِ بدونِ
> پنل یا مهاجرتِ محتوا لازم است.

* **مقاله:** قلمی به `data/articles.php` (`slug` لاتین و یکتا، `date` برای ترتیب، `date_fa` برای نمایش، `blocks` برای بدنه).
* **درس:** در `data/course.php` داخل `stages[x]['lessons']`؛ فیلدهای استاندارد: `goal`، `minutes`، `blocks`، `drill` (با `success` = معیارِ سنجش)، `prerequisite` (نامکِ درسِ پیش‌نیاز) و `refs` (منابع). با افزودن `drill`، جعبه‌ی «تمرین همین درس» و شمارش پیشرفت خودکار فعال می‌شود.
* **آزمونِ مرحله و پروژه‌ی نهایی:** `stages[x]['assessment']` (عنوان + فهرستِ معیارها) و در سطحِ دوره `project` (عنوان، توضیح، معیارهای پذیرش، خروجی) و `prereq` (یادداشتِ پیش‌نیازِ دوره) در `data/course.php`.
* **تمرین / نکته:** `data/exercises.php` (برای تولیدگر موضوع، `'tool' => 'timer+topics'` و `topics`) و `data/tips.php`.

انواع بلوک: `h2`، `h3`، `p`، `lead`، `ul`، `ol`، `quote` (با `by`)، `tip` (با `tone`: `tip|warn|check|idea`)، `table` (با `head` و `rows`)، `drill` (با `title`، `time`، `items`، `note`).
در متن‌ها `**مهم**`، `*تأکید*` و `` `کد` `` پشتیبانی می‌شود.

---

## ۵) امنیت و کیفیت کد

* **مسیریابی با فهرست مجاز** (`routes()` در `includes/helpers.php`): نام مسیر هیچ‌وقت مستقیم به `include` نمی‌رود → امکان LFI نیست.
* **خروجی‌ها** همه با `e()` (`htmlspecialchars` با `ENT_QUOTES|ENT_SUBSTITUTE`)؛ `slug` با `slugify()` به `[a-z0-9_-]` محدود و `param()` به ۲۰۰ نویسه کرپ‌د می‌شود.
* **فرم تماس:** CSRF (`hash_equals`) + فیلد زنبوری + محدودیت نرخ بر پایه‌ی IP + اعتبارسنجی طول/قالب + حذف کاراکترهای کنترلی و تزریق سرصفحه + `flock` هنگام نوشتن CSV.
* **جلسه (session)** فقط برای صفحه‌ی تماس شروع می‌شود (کوکی `HttpOnly`، `SameSite=Lax`، `Secure` روی HTTPS)؛ بقیه‌ی صفحه‌ها بی‌جلسه و کش‌پسندند.
* **سرصفحه‌ها:** `Content-Security-Policy` (با `script-src 'self'`)، `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, `Permissions-Policy`, `Cross-Origin-Opener-Policy` از سمت PHP و به‌عنوان لایه‌ی دوم در `.htaccess`؛ `X-Powered-By` حذف و `ServerSignature Off`.
* **محدودیت نرخِ درست:** پنجره‌ی ثابت با بازنشانیِ واقعی پس از انقضا، حداقل فاصله‌ی بین دو ارسال، خواندن-تغییر-نوشتنِ اتمیک با `flock`، پاک‌سازی خودکارِ باکت‌های رهاشده و سقفِ تعداد فایل؛ اگر `storage` نوشتنی نباشد، **شکست به سمتِ بسته** (fail-closed) است.
* **CSRF:** توکن با `hash_equals`، انقضای ۸ ساعته و بررسی هم‌مبدأ بودنِ `Origin`/`Referer`.
* **نشست:** `use_strict_mode`، کوکی `HttpOnly`/`SameSite=Lax`/`Secure` روی HTTPS و مسیرِ نشستِ پشتیبان وقتی `session.save_path` هاست خالی یا غیرقابل‌نوشتن است.
* **آپلودِ فایل:** فهرستِ سفیدِ MIME (PDF/JPG/PNG/WebP/MP4/WebM/OGV/MP3/M4A/OGG/WAV/WebA)،
  تأیید با `finfo` روی محتوای واقعیِ فایل (دفاع در برابر polyglot)، نامِ تصادفی با پسوندِ
  مشتق‌شده از MIME، سقفِ حجم از config و سنجش با سقفِ واقعیِ PHP، و `uploads/.htaccess`
  با `php_flag engine off` + `Require all denied` برای هر اسکریپت.
* **احراز هویت و نقش‌ها:** گیتِ مرکزی در `bootstrap.php` بر پایه‌ی `routes()`؛ صفحه‌های
  `auth`/`admin` بدونِ ورود/نقشِ درست هرگز رندر نمی‌شوند، همه‌ی فرم‌های POST توکن CSRF
  دارند و مدیرِ اولیه فقط یک‌بار (وقتی هیچ کاربری نیست) ساخته می‌شود.
* **مسیرِ فایل و نشانی‌ها:** `ha_safe_media_url()` و `ha_safe_file_url()` هر `..`، نقطه‌ی
  آغازین، کاراکترِ کنترلی و schemeِ ناشناخته (`javascript:`، `data:`) را رد می‌کنند؛
  امبدِ ویدیو فقط از فهرستِ سفیدِ میزبان‌ها (هم‌راستا با `frame-src` در CSP).
* **بدون وابستگی:** بدون Composer، بدون jQuery، بدون مرحله‌ی build و **بدون Node در محیط عملیاتی**؛ فونت Vazirmatn به‌صورت محلی میزبانی می‌شود (یک فایل woff2 متغیر، بدون درخواست به سرویس خارجی) و آیکون‌ها SVG درون‌خطی‌اند.

### خودآزمایی (بدون وابستگی)
```bash
php tools/selfcheck.php            # محیط، پیکربندی، ذخیره‌سازی، دارایی‌ها، منطقِ محدودیت نرخ، کنتراست WCAG
php tools/selfcheck.php --http     # + آزمونِ دودِ همه‌ی مسیرها، پویش XSS/LFI و سرصفحه‌های امنیتی
```
خروجی، PASS/FAIL است و کدِ خروج در صورتِ هر FAIL برابر ۱ می‌شود (قابل استفاده در CI).
آخرین اجرا: **۱۸۶ PASS / ۰ FAIL / ۱ SKIP** (بدونِ `--http`) روی PHP 8.3؛ با `--http`
آزمونِ دودِ همه‌ی مسیرها، پویشِ XSS/LFI و سرصفحه‌های امنیتی هم اضافه می‌شود.

### اجرای محلی
```bash
cd HAvoice
php -S localhost:8000     # سپس http://localhost:8000
```

---

## ۶) مجوز

کد و محتوا برای استفاده‌ی آزاد آماده است؛ در صورت انتشار مجدد، پیوند این مخزن را نگه دارید.
