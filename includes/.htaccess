# قالب‌ها و توابع — فقط include داخلی، هرگز اجرای مستقیم از وب
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
Options -Indexes
