<?php
/**
 * HAvoice Admin — ویرایش دوره
 *
 * دوره‌ها و درس‌ها ساختارِ تودرتو (stages → lessons) دارند و در
 * data/course.php نگهداری می‌شوند؛ فرمِ تک‌فایلیِ این پنل برای آن
 * ساختار امن نیست (خطرِ خراب‌شدنِ کلِ محتوا). بنابراین این route
 * به‌جایِ رندرِ یک فرمِ ناقص، کاربر را با پیامِ راهنما به فهرستِ
 * دوره‌ها برمی‌گرداند.
 *
 * نکته: نبودِ این فایل باعث می‌شد route «admin_course_edit» به ۴۰۴
 * بیفتد؛ فایل برای کامل‌بودنِ جدولِ routes() ساخته شد.
 */

if (!defined('HA_ROOT')) {
    exit('دسترسی مستقیم ممنوع است.');
}

auth_require_admin();

flash('error', 'ویرایشِ دوره از فایلِ data/course.php انجام می‌شود؛ از همین پنل قابلِ ویرایش نیست.');
redirect(url('admin_courses'));
