# 📚 گزارش کاملِ ممیزی و تکمیلِ ساختار آموزشی HAvoice

> تاریخ: ۱۴۰۵/۰۶/۲۴ (2026-09-15) · شاخه: `arena/01a0a359-havoice` · PHP 8.3 بدون دیتابیس (سازگار با هاست اشتراکی)
>
> اصلِ راهنما در همه‌ی تصمیم‌ها: **کیفیت > ترتیب > کاربرد > تمرین > تعداد.**

---

## ۱) 🧪 خلاصه‌ی اجرایی

| سنجه | قبل | بعد |
|---|---|---|
| حوزه (دسته) | ۱۲ (همه حفظ شد) | ۱۲ ✅ |
| دوره | ۶ | ۶ (بدون حذف/تغییر نامک) ✅ |
| مرحله | ۸ | ۹ (+ comm-2) |
| درس | ۱۶ | ۲۲ (+۶ درسِ ضروری، بدون درسِ پرکننده) |
| تمرین | ۸ (بدون فیلد course/lesson) | ۲۴ (همه با course+lesson) |
| پیوندِ درس↔تمرین | **۰ از ۱۶** | **۲۲ از ۲۲** ✅ |
| عمقِ محتوا (کلماتِ بلوک‌ها) | ۴۲۲ کل / میانگین ۲۶ | ۲,۴۸۷ کل / میانگین ۱۱۳ (۵.۹×) |
| معیارِ سنجشِ تمرین (`success`) | ۰ | ۲۲/۲۲ |
| «اشتباه رایج» در درس | ۳ درس | ۲۲/۲۲ |
| «مثال» در درس | ۲ درس | ۲۲/۲۲ |
| منبع (`refs`) | ۰ | ۱۳ درسِ دارای ادعای علمی |
| پیش‌نیازِ درس | ۰ | ۹ درس + ۱ دوره |
| آزمونِ مرحله (`assessment`) | ۰ از ۸ | ۹ از ۹ ✅ |
| پروژه‌ی نهایی (`project`) | ۰ از ۶ | ۶ از ۶ ✅ |
| **درصدِ کامل‌بودنِ ساختارِ درس** | **≈ ۴۸٪** | **≈ ۹۷٪** |
| **درصدِ کامل‌بودنِ ساختارِ دوره (آزمون/پروژه)** | **۰٪** | **۱۰۰٪** |

فرمولِ درصدِ درس: ۱۳ مؤلفه‌ی استاندارد (عنوان، هدف، سطح، زمان، توضیحِ عمیق، مثال، اشتباه رایج، نکته کلیدی، تمرین، معیار سنجش، نتیجه‌ی قابل‌مشاهده، تمرینِ مرتبط در سیستم، مرجع) برای تک‌تکِ درس‌ها؛ «مرجع» فقط جایی که ادعای علمی وجود دارد نمره‌ی کامل می‌گیرد (عمداً برای همه الزامی نشده تا ارجاعِ تزئینی ساخته نشود).

---

## ۲) 🔗 نقشه‌ی نهایی: حوزه → دوره → مرحله → درس → تمرین

### ۲.۱ فن بیان و سخنوری — مسیرِ پایه (`public-speaking-fundamentals`) · ۳ مرحله / ۱۰ درس / ۱۱۳ دقیقه / ۱۲ پیوندِ تمرین

| مرحله | درس‌ها (نامک) | تمرین‌های پیوسته |
|---|---|---|
| ۱. پایه: نفس، صدا، بدن (۴ درس) | `breathing-foundations`، `resonance-and-power`، `body-supports-words`، `rhythm-and-pause` | breath-46 · lip-trill-ladder · mirror-body-check · filler-counter + pause-marks |
| ۲. ساختار: چه بگوییم و به چه ترتیب (۳ درس) | `opening-hook` (پیش‌نیاز: rhythm-and-pause)، `three-point-body`، `ending-and-ask` | three-opening-patterns · three-point-summary · closing-line-rewrite |
| ۳. اجرا: استرس، تعامل، بداهه (۳ درس) | `nerves-into-energy`، `hard-questions`، `impromptu-sixty` (پیش‌نیاز: hard-questions) | pre-performance-protocol · bridge-five-questions · sixty-second-topic + record-compare |

- آزمونِ مراحل: ۱) ضبطِ ۲۵۰ کلمه‌ای با ۳ معیار · ۲) موضوعِ تدارک‌ندیده با ۱ دقیقه فکر و ۳ دقیقه اجرا · ۳) آزمونِ نهایی ۳×۹۰ ثانیه با نمره‌ی ساختار ۳ + وضوح ۳ + اجرا ۴ (عبور ≥ ۸).
- پروژه‌ی نهایی: سخنرانیِ ضبط‌شده‌ی ۳ دقیقه‌ای با ۴ معیارِ پذیرش و تحویلِ مشخص.
- همه‌ی ۱۲ زیرموضوعِ اصلی فن بیان (تنفس دیافراگمی، رزونانس، زبان بدن، شروع قدرتمند، بدنه‌ی سه‌نقطه‌ای، خلاصه‌گویی/ریتم و مکث، پایان‌بندی، استرس، تعامل، سؤال دشوار، بداهه، اجرا) سرِ جای خود هستند ✅

### ۲.۲ سایر دوره‌ها

| دوره | حوزه | مرحله | درس‌ها | دقیقه | تمرین | آزمون/پروژه |
|---|---|---|---|---|---|---|
| `communication-essentials` ارتباط مؤثر | communication | ۲ | active-listening-foundations · feedback-sbi · **hard-conversations** ➕ | ۲۹ | listen-restate-three · sbi-feedback-ninety · hard-talk-script | ✅ / ✅ |
| `psychology-self-awareness` خودشناسی | psychology | ۱ | labeling-emotion · thought-feeling-behavior · **reframe-self-talk** ➕ | ۲۹ | emotion-labeling-log · cognitive-triangle-table · self-talk-reframe | ✅ / ✅ |
| `habits-success-foundation` عادت و تمرکز | success | ۱ | atomic-habit-two-min · **deep-work-block-lesson** ➕ | ۱۸ | two-minute-habit-week · deep-work-block | ✅ / ✅ |
| `goals-time-management` هدف‌گذاری | goals-time | ۱ | smart-goal-lite · **time-blocking-mit** ➕ | ۱۸ | smart-ifthen-goal · time-block-mit | ✅ / ✅ |
| `negotiation-persuasion` مذاکره (سطح متوسط، با یادداشتِ پیش‌نیازِ دوره‌ی فن بیان) | negotiation | ۱ | batna-preparation · **anchoring-objective-criteria** ➕ | ۲۲ | negotiation-prep-sheet · anchor-with-reasons | ✅ / ✅ |

۱۲ حوزه‌ی موجود دست‌نخورده مانده‌اند (از جمله حوزه‌های محتواییِ books/research/podcast/video که ماهیتِ آن‌ها رسانه است نه دوره). فیلترِ حوزه → دوره کار می‌کند (تستِ HTTP مربوط PASS).

---

## ۳) ⚠️ تحلیل شکاف (قبل) و راه‌حل

| # | شکاف | اقدام |
|---|---|---|
| ۱ | ❌ صفر پیوندِ درس↔تمرین: هر ۸ تمرینِ موجود فیلدِ course/lesson نداشتند | ➕ فیلد به ۸ تمرینِ موجود اضافه شد + ۱۶ تمرینِ جدید → ۲۴ تمرین، پوششِ ۲۲/۲۲ درس |
| ۲ | ❌ هیچ درسی معیارِ سنجش («از کجا بفهمم جواب داده؟») نداشت | ➕ `drill.success` در هر ۲۲ درس — نشانه‌ی قابل‌مشاهده به‌جای حسِ کلی |
| ۳ | ❌ محتوای درس‌ها اسکلتی بود (میانگین ۲۶ کلمه) | ➕ عمق‌بخشی همه‌ی درس‌ها: مثالِ مشخص، اشتباهِ رایج، نکته‌ی کلیدی (میانگین ۱۱۳ کلمه، ×۵.۹) |
| ۴ | ❌ آزمونِ مرحله و پروژه‌ی نهایی وجود نداشت — «پایان دوره» بی‌معنا بود | ➕ ۹ آزمونِ مرحله + ۶ پروژه‌ی نهایی با معیارِ پذیرش و خروجیِ مشخص |
| ۵ | ⚠️ ادعاهای علمیِ بدونِ ردیابی (مثل «حافظه‌ی کاری ۷±۲») | ✅ اصلاح/جایگزینی با ادعاهای مستند + ارجاع (Cowan 2001، Lieberman 2007، Gollwitzer، Clark & Fox Tree 2002، Leroy 2009، Tversky & Kahneman، روزنبرگ، Stone/Patton/Heen، Newport، Clear) |
| ۶ | ⚠️ ترتیبِ آموزشی ناقص: شروعِ قدرتمند قبل از ریتم و مکث می‌آمد؛ بداهه قبل از سؤالِ دشوار | ✅ زنجیره‌ی پیش‌نیاز صریح (۹ درس) + نمایشِ پیش‌نیاز در صفحه‌ی درس |
| ۷ | ⚠️ شماره‌ی درس سراسری بود نه درون‌دوره‌ای («درس ۱۴ از ۲۲») و صفحه‌بندِ درس بین دوره‌ها می‌پرید | ✅ `coursePosition/courseTotal` درون‌دوره‌ای + صفحه‌بند محدود به دوره (تستِ مستقیم PASS) |
| ۸ | ⚠️ دوره‌های کوچک ۱–۲ درسی بدون جمع‌بندی | ➕ یک درسِ کاربردیِ ضروری برای هرکدام (گفت‌وگوی سخت، بازسازیِ خودگویی، کارِ عمیق، زمان‌بندی، لنگرِ اخلاقی) — نه برای بالا بردنِ تعداد؛ هر درس یک مهارتِ مکملِ واقعی است |
| ۹ | ⚠️ شمارنده‌های ناسازگار: کارتِ دوره‌ی اصلی «۹ درس / ۹۹ دقیقه» می‌گفت ولی واقعیت ۱۰ درس / ۱۱۳ دقیقه است | ✅ هم‌ترازیِ `lessons`/`minutes` همه‌ی دوره‌ها با مجموعِ مراحل + اصلاحِ README |
| ۱۰ | ⚠️ ذخیره‌ی پنل، فیلدهای جدید را می‌زدود | ✅ نرمال‌سازِ `admin/course_save.php` اکنون prerequisite/refs/assessment/project/prereq را حفظ می‌کند (تستِ رفت‌وبرگشتِ پنل: ۱۸/۱۸ PASS) |

### درس‌های جدید (فقط ۶ مورد، هر کدام با توجیهِ شکاف)

`impromptu-sixty` (بداهه — از زیرموضوع‌های اصلیِ فن بیان که دوره نداشت) · `hard-conversations` (تنها حلقه‌ی گمشده‌ی زنجیره‌ی شنیدن→بازخورد→گفت‌وگوی سخت) · `reframe-self-talk` (پایانِ طبیعیِ مثلثِ شناختی) · `deep-work-block-lesson` (عادت بدونِ زمانِ اجرا ناقص است) · `time-blocking-mit` (هدف بدونِ تقویمِ اجرا ناقص است) · `anchoring-objective-criteria` (BATNA بدونِ لنگرِ معیارمند ناقص است).

---

## ۴) 📚 استانداردِ ساختارِ درس (که اکنون برقرار است)

هر درس: **عنوان · هدف · سطح (تراشه‌ی سطح در صفحه‌ی درس) · زمان · توضیحِ چندبخشی · مثالِ مشخص · اشتباهِ رایج (tip با tone=warn) · نکته‌ی کلیدی · تمرینِ عملی · معیارِ سنجش (`success`) · نتیجه‌ی قابل‌مشاهده · تمرینِ مرتبط در سیستم (کارتِ تمرینِ پیوسته در پایینِ درس) · مرجع (در صورتِ ادعای علمی)** + پیش‌نیاز با پیوندِ مستقیم به درسِ قبلی.

ساختارِ داده (قراردادِ قالب): درس: `prerequisite`, `refs[]`، تمرین: `drill.success`، مرحله: `assessment => [title, items[]]`، دوره: `project => [title, description, criteria[], deliverable]`, `prereq`.

---

## ۵) 🧪 نتایج آزمون‌ها (بعد از همه‌ی تغییرات)

| آزمون | نتیجه |
|---|---|
| Lint سینتکسِ PHP (معادلِ `php -l` روی همه‌ی فایل‌ها، PHP 8.3) | **۱۱۰ OK / 0 FAILED** ✅ |
| `tools/selfcheck.php` (آفلاین، همانِ CI) | **۱۰۲ PASS / 0 FAIL** (+1 SKIP مربوط به HTTP که جداگانه اجرا شد) ✅ |
| دودِ HTTP کامل (smoke؛ مهمان، دسته، ثبت‌نام/ورود، ۶ صفحه‌ی دوره، ۲۲ صفحه‌ی درس، تمرین‌ها و فیلترها، sitemap/robots/api، 404ها، JSON-LD، XSS/LFI) | **۳۰۸ PASS / 0 FAIL** ✅ |
| تستِ رندرِ عناصرِ جدید (آزمونِ مرحله، پروژه، منابع، پیش‌نیاز، تراشه‌ی سطح) | **۹۲ PASS / 0 FAIL** ✅ |
| تستِ رفت‌وبرگشتِ پنلِ مدیریت (حفظِ فیلدها هنگامِ ویرایش) | **۱۸ PASS / 0 FAIL** ✅ |
| تستِ شماره‌گذاریِ درون‌دوره‌ای و صفحه‌بند | ۵/۵ نمونه صحیح ✅ |
| ساختار (inspect): یتیم‌بودنِ درس/تمرین، سلامتِ نامک‌ها | ۰ یتیم؛ ۲۲/۲۲ درس دارای تمرین؛ ۲۴/۲۴ تمرین دارای course+lesson ✅ |
| **CI (GitHub Actions، PHP 8.3)** | مراحلِ CI عیناً به‌صورتِ محلی معادل‌سازی و اجرا شد (lint+selfcheck+HTTP) — همه سبز؛ اجرای خودکار روی PR انجام می‌شود ⚠️ (اتصالِ GitHubِ این محیط خطای 401 می‌دهد؛ نیاز به اتصالِ مجدد دارد) |

---

## ۶) ✅ فایل‌های تغییریافته (۱۰ فایل، +۹۲۵/−۸۰)

| فایل | تغییر |
|---|---|
| `data/course.php` | عمق‌بخشیِ ۲۲ درس + ۶ درسِ جدید + ۹ آزمونِ مرحله + ۶ پروژه + ۹ پیش‌نیاز + ۱۳ ارجاع + اصلاحِ شمارنده‌ها و بلوکِ سازگاری |
| `data/exercises.php` | +۱۶ تمرین (در مجموع ۲۴)؛ همه با فیلدِ course/lesson |
| `pages/lesson.php` | شماره‌ی درون‌دوره‌ای، تراشه‌ی سطح، پیوندِ پیش‌نیاز، بخشِ منابع |
| `pages/course.php` | جعبه‌ی آزمونِ مرحله، بخشِ پروژه‌ی نهایی، یادداشتِ پیش‌نیازِ دوره |
| `pages/admin/course_save.php` | حفظِ فیلدهای جدید در نرمال‌ساز |
| `pages/admin/course_edit.php` | فیلدِ «پیش‌نیازِ دوره» در فرم |
| `includes/content.php` | نمایه‌سازیِ درون‌دوره‌ای (coursePosition/courseTotal) و همسایه‌های محدود به دوره |
| `includes/helpers.php` | پشتیبانیِ `course_find_lesson` از مختصاتِ دوره |
| `assets/css/style.css` | استایلِ `.drill__success`، `.lesson-refs`، `.lesson__prereq`، `.stage-assessment`، `.course-project`، `.course-prereq` با توکن‌های موجود |
| `README.md` | به‌روزرسانیِ شمارنده‌ها و مستندسازیِ قراردادِ فیلدها |

**چیزی حذف نشده است**: هیچ دوره، مرحله، درس، تمرین، نامک، مسیر یا قابلیتی از بین نرفته؛ معماریِ PHP خالص، مسیریابی، پنل، احراز هویت، سیستم وضعیت، CSS/JS و رفتارِ موبایل دست‌نخورده است.

---

## ۷) ⚠️ ضعف‌های باقی‌مانده (صادقانه)

1. **عمقِ محتوایی درس‌های کوتاه**: میانگین ۱۱۳ کلمه برای درس‌های «ده‌دقیقه‌ای» هنوز در حدِ «راهنمایِ فشرده» است؛ گامِ بعدیِ طبیعی، گسترشِ بلوک‌های متن به ۲۰۰–۳۰۰ کلمه در هر درس است — بدونِ تغییرِ ساختار.
2. **۵ درسِ زیرِ ۱۰۰ کلمه** (عمدتاً درس‌های صوتیِ مرحله‌ی ۱ فن بیان) که ذاتاً تمرین‌محورند و متنِ کوتاه‌تری به‌گونه می‌گیرند.
3. **آزمون‌ها خودارزیابی‌اند**: معیارها رفتاری و قابل‌ مشاهده‌اند (ضبط، چک‌لیست) اما سیستم نظارت/ثبتِ نمره ندارد؛ پیشرفتِ موجود در localStorage همان است که بود.
4. **۶ حوزه بدونِ دوره** (life-skills، career و حوزه‌های رسانه‌ای) — ساختار برای افزودنِ دوره آماده است؛ افزودنِ محتوایِ بی‌کیفیت فقط برای پوششِ دسته‌ای، خلافِ اصلِ «کیفیت > تعداد» است و انجام نشد.
